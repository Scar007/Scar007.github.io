# hr
离职原因、期望薪资 等

# 一面
1. 一个div，不知道宽高，想要实现一个垂直居中布局，都有哪些方式？
2. this问题
3. ES6知识点
4. vue中vue-router是怎么实现的
5. webpack了解吗？都做过哪些事情？
6. webpack的loader和plugin都有哪些？
7. loader的加载顺序是怎么样的？
8. JS事件执行，什么是宏任务，什么是微任务
```js
var a = 1
var obj = {
  a: 2,
  getA: () => {
    return this.a
  }
}
console.log(obj.getA())
```
9. this指向问题
10. ES6的箭头函数的this是怎么样的？
```js
function t1() {
  var age = 20
  function t2() {
    console.log(age)
  }
  return t2
}
var tmp = t1()
var age = 100
tmp()
```
11. generator函数了解吗？
```js
function* foo() {
  yield 1;

  return 6;
}
foo();

for(let v of foo()) {
  console.log(v)
}
```
12. 介绍一下promise
13. JS事件执行时怎么样的？
```js
new Promise((resolve, reject) => {
  console.log(1)
  setTimeout(() => {
    console.log(2)
  })
  resolve();
}).then(() => {
  console.log(3)
}).then(() => {
  return new Promise((resolve, reject) => {
    console.log(4)
  }).then(() => {
    console.log(5)
  })
}).then(() => {
  console.log(6)
})
console.log(7)
```


## 编程题
1. 传入一个数，将这个数拆成最多个数相乘的数组，如：
```
8 --> [2, 2, 2]
30 ---> [2, 3, 5]
```

2. 实现一个方法，具备以下能力：
- 支持全局配置能力，全局配置能配置请求分类以及请求分类的优先级，配置baseUrl，headers选项
- 同时发送请求只能有6个
- 能控制请求的优先级，优先级高的请求先发起

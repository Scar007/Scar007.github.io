# 一面
1. 自我介绍
2. new 操作符做了什么？<br>
追问：下边的new A会输出什么，为什么？
```js
function  A () {
    this.a = 123;
    return 456;
}
// new A() = > {a: 123}
new A() => 456;
```
3. 原型链的相关知识
```js
Object.prototype.a = 'Object';
Function.prototype.a = 'Function';
function Person () {};
var child = new Person();
console.log(child.a);
console.log(Person.a);
```
追问：这种创建方式可以吗？
```js
let Person = new Function()
```
4. JS事件执行<br>
追问：打印出下边的东西<br>
```js
function bar() {
  console.log('bar')
  Promise.resolve().then((str) => console.log('micro-bar'))
  setTimeout((str) => console.log('macro-bar'), 0) 
}

function foo() {
  console.log('foo')
  Promise.resolve().then((str) => console.log('micro-foo'))
  setTimeout((str) => console.log('macro-foo'), 0)
  bar()
}

foo()
console.log('global')
Promise.resolve().then((str) => console.log('micro-global'))
setTimeout((str) => console.log('macro-global'), 0)
```
5. 什么是堆栈溢出？<br>
追问：如何解决堆栈溢出<br>
追问：优化封装下列函数<br>
```js
function factorial(n) {
  if (n <= 1) return 1;
  return n * factorial(n - 1);
}
factorial(5000)
```
6. 浏览器加载资源会有什么限制？<br>
7. 介绍一下HTTP<br>
Http1特点，优缺点<br>
Http2特点，优缺点<br>
http3？<br>
8. 跨域<br>
JSONP形式script.src传的是什么，拿到的是什么？
```js
script.src="http://a.com/getInfo
                        /xxx.js"
```
重点询问CORS(包括简单请求和复杂请求)和Nginx


# 二面
## 简答题
1. 最近的项目中，自己担任的角色，做了哪些事情？
2. 项目流程
3. 技术选型
4. 方案规划
5. 项目优化点
6. 上线部署流程
7. Jenkins做了什么
8. 如何区分各种环境的
9. 发布npm包，怎么区分环境的，在测试环境验证完的包，如何在线上环境使用的
10. webpack怎么做的环境区分
11. webpack文件打包怎么做的
12. 小程序做过吗？...
13. 现在薪资、职级
14. 离职原因

## 编程题
```js
["a", "abc", "ab", "aa", "aa"] list
1. 按字符串长度逆序排序
2. 长度相同，按字符串ASCII码顺序排序
输出：
abc
aa
aa
ab
a
```


# 三面
1. 前端自己怎么去做数据统计，计算PV、UV(不借助神策等，或者说神策怎么计算的PV、UV)
2. CDN加载过程
3. 浏览器缓存在项目中具体是怎么做的？


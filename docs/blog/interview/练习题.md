---
publish: false
title: 练习题
createTime: 2025/03/24 16:49:29
permalink: /blog/44gq6zmj/
---

### 用promise写一个异步函数(百度)
```js
function fetchData(value) {
  return new Promis((resolve,reject) => {
    setTimeour(() => {
      const res = true
      if (res) {
        resolve({ data: 'success' })
      } else {
        reject({ data: 'failed' })
      }
    }, 1000)
  })
}

fetchData().then(res => {
  console.log(data)
}).catch((err) => {
  console.log(err)
})
```

### 括号匹配
```js
function matchBrackets(str) {
  const arr = []
  const bracketStr = {
    '}' : '{',
    ']' : '[',
    ')' : '('
  }

  for(let char of str) {
    if (chat === "") {

    }
  }
}
```
---
publish: false
title: 伴鱼
createTime: 2024/04/06 21:39:00
permalink: /blog/oqtxo1rb/
---

# 一面
1. ES6相关所有点
2. class类
3. 什么是面向对象
4. promise
5. http
6. 作用域了解吗？都分为哪些？
7. 浏览器缓存
8. 强缓存、协商缓存
9. 为什么有Etag还要有Last-Modified，有什么区别，分别是解决什么问题的？
```js
最长不重复子串

请从字符串中找出一个最长的不包含重复字符的子字符串，计算该最长子字符串的长度。

示例 1:
输入: "abcabcbb"
输出: 3 
解释: 因为无重复字符的最长子串是 "abc"，所以其长度为 3。

示例 2:
输入: "bbbbb"
输出: 1
解释: 因为无重复字符的最长子串是 "b"，所以其长度为 1。

示例 3:
输入: "pwwkew"

输出: 3
解释: 因为无重复字符的最长子串是 "wke"，所以其长度为 3。
     请注意，你的答案必须是 子串 的长度，"pwke" 是一个子序列，不是子串。

funtion fn(str) {
let arr = [];
let res = ''
for(let i = 0; i<str.length; i++) {
   if (res.includes(str[i])) {
     let index = res.indexOf(str[i])
     arr.push(res.splice(index, 1))
   } else {
     res+=str[i]
   }
}
arr.find(item => Math.max(item.length))  
}

```


---
publish: false
title: 快手
createTime: 2024/04/06 21:39:00
permalink: /blog/7d757mpd/
---

# 一面
1. 自我介绍
2. 介绍一下自己最熟悉的项目，包括遇到的各种印象深刻的问题等

```js
var a = [1, 2, 3, 4];
function set(a) {
  a = [5, 6, 7, 8];
}
set(a);
console.log(a); // [1,2,3,4]
```

```js
var name = '123';
var obj = {
  name: '456',
  getName: function () {
    function printName () {
      console.log(this.name);
    }
    printName();
  }
}
obj.getName(); 
```

// 123

```js
Function.prototype.a = () => alert(1);
Object.prototype.b = () => alert(2);
function A() {};
var a = new A();
a.a(); // 1
a.b(); // 报错
```

```css
<style>
  .classA { color:blue; }
  .classB { color:red; }
</style>
<p class="classB classA">hello</p>
```

```css
<style>
  p {
    background: red;
    color: #000;
    /* position: absolute; */
  }
</style>
<div>
  <span>11111</span>
  <p>2222<p>
  <span>3333</span>
</div>
```
// 无absolute：各占一行
// 有absolute：1、3同行，2盖在上面


实现页面效果时印象深的case：
1. 实现动画：css animation；自主封装的基于webgl的动画库，实现元素跑动、跳动效果
2. v-scale缩放指令，解决分辨率区别较大的各设备上的适配问题

补全repeat：
const repeatFunc = repeat(console.log, 4, 3000);
repeatFunc("helloworld");

// 每3秒打印一个helloworld，总计执行4次